# AIron Audit Runner

Backend de auditoria en vivo (nmap / whatweb / nuclei) contra activos propios de Farguell
(lista blanca), con consola web en directo (SSE). Desplegado en Coolify con Basic Auth.

Editar objetivos en `TARGETS` dentro de `app.py`.
